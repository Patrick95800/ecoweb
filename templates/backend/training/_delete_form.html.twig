<form method="post" action="{{ path('backend_trainings_delete', {'id': training.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette formation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
