{% extends 'backend/base.html.twig' %}

{% block title %}Edition de la formation {{ training.title }}{% endblock %}

{% block body %}
    <h1>Edition de la formation {{ training.title }}</h1>

    <p>{{ include('backend/training/_form.html.twig', {'button_label': 'Mettre à jour'}) }}</p>

    <p>{{ include('backend/training/_delete_form.html.twig') }}</p>

    <p><a href="{{ path('backend_trainings') }}" class="btn btn-secondary" role="button">Retour à la liste</a></p>
{% endblock %}
