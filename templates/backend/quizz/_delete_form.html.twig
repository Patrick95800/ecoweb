<form method="post" action="{{ path('backend_quizzes_delete', {'id': quizz.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce quizz ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quizz.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
