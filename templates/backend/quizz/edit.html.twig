{% extends 'backend/base.html.twig' %}

{% block title %}Edition de quizz{% endblock %}

{% block body %}
    <h1>Edition de quizz</h1>

    <p>{{ include('backend/quizz/_form.html.twig', {'quizz': quizz, 'button_label': 'Mettre à jour'}) }}</p>

    <p>{{ include('backend/quizz/_delete_form.html.twig') }}</p>

    <p><a href="{{ path('backend_quizzes') }}" class="btn btn-secondary" role="button">Retour à la liste</a></p>
{% endblock %}
