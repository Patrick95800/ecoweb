<form method="post" action="{{ path('backend_teacher_requests_delete', {'id': teacher_request.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette demande ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teacher_request.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
